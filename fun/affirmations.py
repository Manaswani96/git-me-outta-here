import random

messages = [
    "You're not behind. You're buffering.",
    "Depth > noise. Always.",
    "Tiny commits still count.",
    "You don’t need to overshare to shine.",
    "Building quietly is still building."
]

if __name__ == "__main__":
    print(random.choice(messages))
